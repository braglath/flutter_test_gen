name: flutter_test_gen_example
description: Example project for flutter_test_gen
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_test_gen:
    path: ..
  mocktail: ^1.0.0

flutter:
  uses-material-design: true
